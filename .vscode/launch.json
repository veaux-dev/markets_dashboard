{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run main_mkt_db.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main_mkt_db.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}